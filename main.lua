app = require("application")  
config = require("config")  
setup = require("setup")

-- Once Setup is complete (and WiFi is connected) app.start() is called
--  in setup.wifi_wait_IP()
setup.start()  

